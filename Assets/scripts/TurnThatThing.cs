using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class TurnThatThing : MonoBehaviour {

	public float speed = 1;
	
	void Update () {
	//	gameObject.transform.rotation.y = gameObject.transform.rotation.y + speed * Time.deltaTime;
		
	}
}
